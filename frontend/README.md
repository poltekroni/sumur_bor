# Frontend React Vite

Frontend untuk pelayanan renovasi rumah dan sumur bor submersible.

## Instalasi

```bash
npm install
cp .env.example .env
npm run dev
```

Pastikan `VITE_API_URL` mengarah ke backend Laravel, misalnya `http://localhost:8000/api`.
